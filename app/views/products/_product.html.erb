<div class="col-lg-3 col-md-6 single-blog">
  <div class="thumb mb-3">
    <%= image_tag get_thumnail(product), class: "img-responsive" %>
  </div>
  <p><%= product.name %></p>
  <%= form_tag carts_path, method: :post, remote: true do %>
    <%= hidden_field_tag :type, "#{Product.name}" %>
    <%= hidden_field_tag :product_id, "#{product.id}" %>
    <%= hidden_field_tag :quantity, "#{Settings.quantity}" %>
    <%= button_tag type: :submit, class: "genric-btn primary-border buton_add mb-2" do %>
      <i class="fa fa-shopping-cart"> </i> <%= t ".add_to_cart" %>
    <% end %>
  <% end %>
  <h4><%= number_to_vnd product.price %></h4>
  <div class="meta-bottom d-flex justify-content-between">
    <p><i class="fa fa-comment"></i> <%= t ".comment", count: count_comments(product) %></p>
  </div>
</div>
