<!DOCTYPE html>
<html>
  <head>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="keywords" content="pizza, delivery food, fast food, sushi, take away, chinese, italian food">
    <meta name="description" content="">
    <meta name="author" content="Ansonika">
    <title>QuickFood - Quality delivery or take away food</title>

    <!-- Favicons-->
    <link rel="shortcut icon" href=<%= image_path("img/favicon.ico") %> type="image/x-icon">
    <link rel="apple-touch-icon" type="image/x-icon" href=<%= image_path("img/apple-touch-icon-57x57-precomposed.png") %>>
    <link rel="apple-touch-icon" type="image/x-icon" sizes="72x72" href=<%= image_path("img/apple-touch-icon-72x72-precomposed.png") %>>
    <link rel="apple-touch-icon" type="image/x-icon" sizes="114x114" href=<%= image_path("img/apple-touch-icon-114x114-precomposed.png") %>>
    <link rel="apple-touch-icon" type="image/x-icon" sizes="144x144" href=<%= image_path("img/apple-touch-icon-144x144-precomposed.png") %>>
    
    <!-- GOOGLE WEB FONT -->
    <link href='https://fonts.googleapis.com/css?family=Lato:400,700,900,400italic,700italic,300,300italic' rel='stylesheet' type='text/css'>
    <link href='http://fonts.googleapis.com/css?family=Gochi+Hand' rel='stylesheet' type='text/css'>

    <link href=<%= stylesheet_path("skins/square/grey.css") %> rel="stylesheet">
    <link href=<%= stylesheet_path("ion.rangeSlider.css") %> rel="stylesheet">
    <link href=<%= stylesheet_path("ion.rangeSlider.skinFlat.css") %> rel="stylesheet" >
    <link href=<%= stylesheet_path("slider-pro.min.css") %> rel="stylesheet">

    <script src=<%= javascript_path("js/modernizr.js") %>></script> 
    <%= render "layouts/shim" %>
    <!--<%= render "layouts/shim2" %>-->
  </head>
  <body>
    <!-- Load Facebook SDK for JavaScript -->
    <div id="fb-root"></div>
      

    <!-- Your customer chat code -->
    <div class="fb-customerchat" attribution=setup_tool page_id="100511921460770" theme_color="#0084ff"></div>
    <div id="preloader">
        <div class="sk-spinner sk-spinner-wave" id="status">
            <div class="sk-rect1"></div>
            <div class="sk-rect2"></div>
            <div class="sk-rect3"></div>
            <div class="sk-rect4"></div>
            <div class="sk-rect5"></div>
        </div>
    </div>
    <!-- End Preload -->
    <% flash.each do |message_type, message| %>
      <%= hidden_field_tag "", message, class: "flash_msg #{message_type}" %>
    <% end %>
    <%= render "layouts/header" %>
    <%= yield :banner %>

      <%= yield %>
    <%= render "layouts/footer" %>
  </body>
</html>
